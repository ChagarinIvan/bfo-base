# Quickstart

1. Create an active cup with a group and an active cup stage linked to an
   event.
2. Request `/api/v1/cup-events?eventIds[]=EVENT_ID`; verify compact context.
3. Open each supported SPA table; verify the icon badge, popover and link.
4. Verify an unlinked event shows no badge.
