# Quickstart

1. Create an event with a protocol and observe the queued/parsing notice.
2. Confirm the detail page refreshes every five seconds through identifying and rebuilding ranks.
3. Confirm ready results stop polling; a parser error shows the authenticated error message.
4. Confirm public list/detail reject no-protocol, in-progress and failed events.
