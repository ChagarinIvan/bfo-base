import { describe, expect, it } from 'vitest'
import EditClubPage from './EditClubPage.vue'

describe('edit club page', () => {
    it('uses the shared club form page component', () => {
        expect(EditClubPage).toBeDefined()
    })
})
